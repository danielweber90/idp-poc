# Kong Ingress Controller values for IDP PoC
# CLB SSL termination is managed directly via AWS CLI (not via K8s annotations)
# to avoid Kubernetes reconciling the listener config.
# See RUNBOOK.md for CLB listener setup.
controller:
  ingressClass: kong
  installCRDs: false

proxy:
  type: LoadBalancer
