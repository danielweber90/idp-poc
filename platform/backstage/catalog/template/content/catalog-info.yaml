apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.name | lower | replace(" ", "-") | replace("_", "-") }}
spec:
  type: service
  owner: user:guest
  lifecycle: experimental
