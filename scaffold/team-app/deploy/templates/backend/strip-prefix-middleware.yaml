# Traefik strips /<teamName>/api before forwarding to FastAPI, so FastAPI routes start at /
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: strip-api-prefix
  namespace: {{ .Values.teamName }}
spec:
  stripPrefix:
    prefixes:
      - /{{ .Values.teamName }}/api
